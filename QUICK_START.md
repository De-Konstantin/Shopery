# Быстрый старт: Интеграция с Backend API

## ✅ Что уже сделано

### 1. Созданы файлы конфигурации

- ✅ `.env` - переменные окружения с URL backend API
- ✅ `.env.example` - шаблон для других разработчиков
- ✅ `.gitignore` - обновлен для исключения .env файлов

### 2. Обновлен API модуль (`src/utils/api.js`)

- ✅ Использует `import.meta.env.VITE_API_URL` для Vite
- ✅ Функции для работы с продуктами: `getProducts`, `getProductById`, `createProduct`, `updateProduct`, `deleteProduct`
- ✅ Вспомогательные функции: `searchProducts`, `getProductsByCategory`, `getPopularProducts` и др.
- ✅ Новая функция `getFilterMetadata()` для получения метаданных фильтров

### 3. Обновлены компоненты

- ✅ **Shop.jsx** - загружает товары с API с фильтрацией и пагинацией
- ✅ **Filter.jsx** - получает метаданные (категории, теги, цены) с API
- ✅ **HotDeals.jsx** - загружает товары со скидками с API
- ✅ **TopProducts.jsx** - загружает популярные товары с API

## 🚀 Следующие шаги

### 1. Настройте URL вашего backend

Откройте файл `.env` в корне проекта и укажите правильный URL:

```env
VITE_API_URL=http://localhost:3000
```

Если ваш backend работает на другом порту или хосте, измените URL соответственно.

### 2. Убедитесь, что backend запущен

Запустите ваш Docker контейнер с backend:

```bash
docker compose up
# или
docker-compose up
```

Проверьте доступность API:

```bash
curl http://localhost:3000/products
```

### 3. Настройте CORS на backend

Backend должен разрешать запросы с фронтенда. Добавьте CORS middleware:

**Для Express.js:**

```javascript
const cors = require('cors');

app.use(
  cors({
    origin: 'http://localhost:5173', // Vite dev server
    credentials: true,
  }),
);
```

**Для NestJS:**

```typescript
app.enableCors({
  origin: 'http://localhost:5173',
  credentials: true,
});
```

### 4. Запустите фронтенд

```bash
npm install  # если еще не установлены зависимости
npm run dev
```

Откройте браузер: `http://localhost:5173`

### 5. Проверьте работу

1. **Главная страница** - должны загружаться товары в секциях "Popular Products" и "Hot Deals"
2. **Страница Shop** (`/shop`) - должна работать фильтрация, пагинация и отображение товаров
3. **Фильтры** - должны подгружаться актуальные категории и теги с backend

## 🔍 Отладка

### Открыть Developer Tools (F12)

**Console Tab:**

- Проверьте наличие ошибок
- При проблемах с загрузкой увидите: "Error loading products: ..."

**Network Tab:**

- Найдите запросы к `/products`
- Проверьте статус ответа (должен быть 200)
- Посмотрите содержимое ответа

### Типичные проблемы

**❌ CORS Error**

```
Access to fetch at 'http://localhost:3000/products' from origin 'http://localhost:5173'
has been blocked by CORS policy
```

**Решение:** Настройте CORS на backend (см. шаг 3)

---

**❌ Network Error / Failed to fetch**

```
Error loading products: TypeError: Failed to fetch
```

**Решение:**

- Проверьте, запущен ли backend
- Проверьте URL в `.env`
- Проверьте доступность через Postman/curl

---

**❌ Товары не отображаются**

- Откройте консоль и проверьте структуру ответа от API
- Убедитесь, что backend возвращает данные в формате:
  ```json
  {
    "items": [...],
    "meta": { "total": 100, "page": 1, ... }
  }
  ```

## 📖 Дополнительная документация

См. подробную документацию в файле `API_INTEGRATION.md`

## 🎉 Готово!

После выполнения всех шагов ваш фронтенд должен успешно работать с backend API.

Товары больше не загружаются из статического `products.json`, а приходят с вашего backend через Docker! 🐳
