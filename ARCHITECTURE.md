# Архитектура интеграции Frontend ↔ Backend

## Схема взаимодействия

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React + Vite)                 │
│                      http://localhost:5173                      │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  │ HTTP Requests
                                  │ (fetch API)
                                  ▼
        ┌───────────────────────────────────────────┐
        │       src/utils/api.js (API Layer)        │
        │  • getProducts(params)                    │
        │  • getProductById(id)                     │
        │  • getFilterMetadata()                    │
        │  • searchProducts(query)                  │
        │  • getPopularProducts()                   │
        └───────────────────────────────────────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    │             │             │
                    ▼             ▼             ▼
        ┌───────────────┐ ┌─────────────┐ ┌────────────────┐
        │   Shop.jsx    │ │ Filter.jsx  │ │  HotDeals.jsx  │
        │               │ │             │ │                │
        │ • Пагинация   │ │ • Категории │ │ • Скидки >= 35%│
        │ • Фильтрация  │ │ • Теги      │ │                │
        │ • Сортировка  │ │ • Цены      │ │  TopProducts   │
        └───────────────┘ └─────────────┘ │ • Рейтинг >=4.5│
                                          └────────────────┘
                                  │
                                  │ CORS enabled
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND API (Docker)                         │
│                  http://localhost:3000                          │
│                                                                 │
│  Endpoints:                                                     │
│  • GET  /products      - список товаров (с фильтрами)          │
│  • GET  /products/:id  - один товар                            │
│  • POST /products      - создать товар                         │
│  • PATCH /products/:id - обновить товар                        │
│  • DELETE /products/:id - удалить товар                        │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
                        ┌──────────────────┐
                        │    DATABASE      │
                        │   PostgreSQL     │
                        │   MySQL          │
                        │   MongoDB        │
                        │   etc.           │
                        └──────────────────┘
```

## Поток данных

### 1. Загрузка страницы Shop

```
User → Shop.jsx → useEffect() → getProducts({page:1, limit:12})
                                      │
                                      ▼
                        api.js → fetch('http://localhost:3000/products?page=1&limit=12')
                                      │
                                      ▼
                              Backend API → Database
                                      │
                                      ▼
                        Response: { items: [...], meta: {...} }
                                      │
                                      ▼
                        Shop.jsx → setProducts(response.items)
                                      │
                                      ▼
                        Render: {products.map(p => <ProductCard />)}
```

### 2. Применение фильтров

```
User → Filter.jsx → onChange → Shop.jsx → setFilters()
                                              │
                                              ▼
                                    useEffect(filters) - срабатывает
                                              │
                                              ▼
                        getProducts({
                          page: 1,
                          category: 'fruits',
                          minPrice: 0,
                          maxPrice: 50,
                          tags: 'organic',
                          rating: 4.5
                        })
                                              │
                                              ▼
                        Backend обрабатывает фильтры в SQL/MongoDB
                                              │
                                              ▼
                        Возвращает отфильтрованные товары
                                              │
                                              ▼
                        UI обновляется с новыми результатами
```

### 3. Пагинация

```
User → Нажимает "Страница 2" → handlePageChange(2)
                                      │
                                      ▼
                        setPagination({ page: 2 })
                                      │
                                      ▼
                        useEffect(pagination.page) - срабатывает
                                      │
                                      ▼
                        getProducts({ page: 2, limit: 12, ...filters })
                                      │
                                      ▼
                        Backend → SELECT * FROM products
                                  WHERE ...
                                  LIMIT 12 OFFSET 12
                                      │
                                      ▼
                        Товары 13-24 возвращаются
                                      │
                                      ▼
                        UI показывает товары 2-й страницы
```

## Структура данных

### Request (от Frontend к Backend)

```javascript
// GET /products?page=1&limit=12&category=fruits&minPrice=0&maxPrice=50

Query Parameters:
{
  page: 1,
  limit: 12,
  category: 'fruits',
  minPrice: 0,
  maxPrice: 50,
  tags: 'organic,fresh',
  rating: 4.5,
  sort: 'price_asc'
}
```

### Response (от Backend к Frontend)

```javascript
{
  items: [
    {
      id: 1,
      productName: 'Green Apple',
      slug: 'green-apple',
      images: ['url1', 'url2'],
      category: 'fruits',
      tags: 'organic,fresh',
      priceOrigin: 4.99,
      weight: 0.5,
      description: 'Fresh green apples',
      quantity: 100,
      discount: 10,
      rating: 4.5,
      isAvailable: true
    },
    // ... еще 11 товаров
  ],
  meta: {
    page: 1,
    limit: 12,
    total: 150,        // всего товаров в БД (с фильтрами)
    totalPages: 13,    // всего страниц
    hasNextPage: true,
    hasPreviousPage: false
  }
}
```

## Конфигурация

### Frontend (.env)

```env
VITE_API_URL=http://localhost:3000
```

### Backend (CORS)

```javascript
// Express.js
app.use(
  cors({
    origin: 'http://localhost:5173',
    credentials: true,
  }),
);

// NestJS
app.enableCors({
  origin: 'http://localhost:5173',
  credentials: true,
});
```

## Обработка ошибок

```javascript
// Frontend
try {
  const data = await getProducts(params);
  setProducts(data.items);
} catch (error) {
  console.error('Error:', error);
  setError('Ошибка загрузки товаров');
}

// Backend должен возвращать:
// - 200 OK - успешный запрос
// - 400 Bad Request - неверные параметры
// - 404 Not Found - товар не найден
// - 500 Internal Server Error - ошибка сервера
```

## Производительность

### Оптимизации:

1. **Серверная пагинация** - загружаем только 12 товаров вместо всех
2. **Серверная фильтрация** - база данных фильтрует быстрее, чем JavaScript
3. **Кэширование** (рекомендуется добавить):
   - React Query / SWR для кэширования ответов
   - Backend кэширование (Redis)

### Пример с React Query:

```javascript
import { useQuery } from '@tanstack/react-query';

const { data, isLoading, error } = useQuery({
  queryKey: ['products', filters, pagination.page],
  queryFn: () => getProducts({ ...filters, page: pagination.page }),
  staleTime: 60000, // кэш на 1 минуту
});
```

## Масштабирование

### Текущая архитектура позволяет:

- ✅ Работать с миллионами товаров (пагинация)
- ✅ Быстрые фильтры (индексы в БД)
- ✅ Легко добавлять новые endpoints
- ✅ Микросервисная архитектура (будущее)

### Возможные расширения:

```
Frontend → Backend API → [Products Service]
                      ↓  [Orders Service]
                      ↓  [Auth Service]
                      ↓  [Cart Service]
                      ↓  [Wishlist Service]
```

## Безопасность

### Рекомендации:

1. **HTTPS** в production
2. **API ключи / JWT токены** для аутентификации
3. **Rate limiting** на backend
4. **Валидация** входных данных
5. **Sanitization** перед записью в БД

### Пример с JWT:

```javascript
// Frontend (api.js)
const response = await fetch(`${API_BASE_URL}/products`, {
  headers: {
    Authorization: `Bearer ${token}`,
    'Content-Type': 'application/json',
  },
});

// Backend
app.use('/products', authenticateJWT, productsRouter);
```

---

**Эта архитектура обеспечивает надежную, масштабируемую и производительную интеграцию между фронтендом и бэкендом!** 🚀
